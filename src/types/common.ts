
// Define a common ObjectId type that can be used across the application
export type ObjectId = string;

// Add other common types as needed
export type Status = "active" | "inactive" | "pending" | "archived";
